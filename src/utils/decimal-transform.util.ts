// import { Produto } from '@prisma/client';

// export function transformProductDecimals(product: Produto) {
//   return {
//     ...product,
//     // preco: Number(product.preco),
//     // subpreco: Number(product.subpreco),
//     // quantidade: Number(product.quantidade),
//     // viws: Number(product.viws),
//     // desconto: Number(product.desconto),
//   };
// }

// export function transformProductsArray(products: Produto[]) {
//   return products.map((product) => transformProductDecimals(product));
// }
